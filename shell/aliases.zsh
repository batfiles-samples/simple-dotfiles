# Installed at ~/.config/zsh/aliases.zsh, two directories from where it lives
# here. The manifest decides the destination; the repository stays flat.

alias ll='ls -l'
alias la='ls -la'
alias gs='git status --short'
alias gd='git diff'
